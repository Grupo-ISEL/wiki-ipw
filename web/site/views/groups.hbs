<div class="justify-content-center">
    <h2>{{title}}</h2>
    <div class="row justify-content-center text-center">
        {{#each groups}}
            {{> groupP}}
            <hr>
        {{/each}}
    </div>
</div>
