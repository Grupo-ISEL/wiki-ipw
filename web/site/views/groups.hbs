<div class="justify-content-center">
    <h2>{{title}}</h2>
    <div class="row justify-content-center text-center">
        {{#each groups}}
            {{> groupP group=this token=../token}}
            <hr class="mt-3 mb-3">
        {{/each}}
    </div>
</div>
